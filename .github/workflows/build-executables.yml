name: Build Executables

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: Foxyiz-linux
            executable_name: Foxyiz
          - os: windows-latest
            artifact_name: Foxyiz-windows
            executable_name: Foxyiz.exe
          - os: macos-latest
            artifact_name: Foxyiz-macos
            executable_name: Foxyiz

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Setup Chrome (Linux)
      if: runner.os == 'Linux'
      uses: browser-actions/setup-chrome@v1
      with:
        chrome-version: stable

    - name: Setup Chrome (macOS)
      if: runner.os == 'macOS'
      run: |
        brew install --cask google-chrome

    - name: Setup Chrome (Windows)
      if: runner.os == 'Windows'
      run: |
        choco install googlechrome -y --ignore-checksums

    - name: Build executable with PyInstaller (Windows)
      if: runner.os == 'Windows'
      run: |
        pyinstaller --onefile --add-data "x/xActions.py;x" --add-data "z/zDash_template.html;z" --collect-all selenium --collect-all webdriver_manager --hidden-import pandas --hidden-import x.xActions --hidden-import numpy --hidden-import selenium --hidden-import selenium.webdriver --hidden-import webdriver_manager.chrome --hidden-import requests --hidden-import urllib3 --hidden-import requests.adapters --hidden-import requests.auth --hidden-import requests.cookies --hidden-import requests.exceptions --hidden-import requests.sessions --hidden-import requests.utils --hidden-import multiprocessing.spawn --hidden-import multiprocessing.semaphore_tracker --name Foxyiz f/fEngine.py

    - name: Build executable with PyInstaller (Unix)
      if: runner.os != 'Windows'
      run: |
        pyinstaller --onefile --add-data "x/xActions.py:x" --add-data "z/zDash_template.html:z" --collect-all selenium --collect-all webdriver_manager --hidden-import pandas --hidden-import x.xActions --hidden-import numpy --hidden-import selenium --hidden-import selenium.webdriver --hidden-import webdriver_manager.chrome --hidden-import requests --hidden-import urllib3 --hidden-import requests.adapters --hidden-import requests.auth --hidden-import requests.cookies --hidden-import requests.exceptions --hidden-import requests.sessions --hidden-import requests.utils --hidden-import multiprocessing.spawn --hidden-import multiprocessing.semaphore_tracker --name Foxyiz f/fEngine.py

    - name: List dist directory (Debug)
      run: |
        ls -la dist/
      shell: bash

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ matrix.artifact_name }}
        path: dist/${{ matrix.executable_name }}
        if-no-files-found: error

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts

    - name: List artifacts (Debug)
      run: |
        ls -la artifacts/
        find artifacts/ -type f

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          artifacts/Foxyiz-linux/Foxyiz
          artifacts/Foxyiz-windows/Foxyiz.exe
          artifacts/Foxyiz-macos/Foxyiz
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

